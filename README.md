# Oryon Web

## How to Clone

* To clone a Git repository along with all of its submodules
    ```bash
    git clone --recurse-submodules git@github.com:qarven/oryon-web.git
    ```
* If you already cloned the project and the submodule folders are empty
    ```sh
    git submodule update --init --recursive
    ```

## Others